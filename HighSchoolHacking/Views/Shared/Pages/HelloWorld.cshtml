@using HighSchoolHacking.Models
@using HighSchoolHacking.Models.Pages
@model HelloWorld

@{
    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Model.Color,
        Title = "Hello World",
        Extension = Model.LanguageLower,
        Large = true,
        Back = "/" + Model.Language + "/Learn",
        Paragraphs = new string[]
        {
            "We're going to spend the first few lessons getting you used to working in " + Model.Language + ". It's " + Model.LanguageDescription + ".",
            "Start by going to <a href='http://repl.it/languages/" + Model.LanguageLower + "'>repl.it/languages/" + Model.LanguageLower + "</a>. This is a 'shell' where you can enter " + Model.Language + " on the left, hit the play button (or Ctrl + Enter) to make it run, and view output on the right.",
            "Enter the following code on the left side of the screen:",
            Section.WrapCode(Model.LanguageLower, Model.PrintFunction + "(\"Hello world!\")"),
            "You should see a <em>Hello world!</em> appear on the right side of the screen. If you did, great! If not, remember to play the code using the button or shortcut.",
            "If you already know how to code, just skim the lessons after this one to learn the syntax.",
        }
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = "steel",
        Title = "Exercises",
        Paragraphs = new string[]
        {
            Section.WrapList(new string[]
            {
                "Instead of printing <em>Hello World</em>, print out your full name."
            })
        }
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = "brown",
        Title = "Solutions",
        Contracted = true,
        Paragraphs = new string[]
        {
            Section.WrapList(new string[]
            {
                "Instead of printing <em>Hello World</em>, print out your full name." + Section.WrapCode("python", new string[]
                {
                    Model.PrintFunction + "(\"Joshua Kevin Goldberg\")"
                })
            })
        }
    });
}
