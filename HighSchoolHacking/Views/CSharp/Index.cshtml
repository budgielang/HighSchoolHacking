@using HighSchoolHacking.Models

@{
    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Languages.CSharp.Color,
        Title = "C#",
        Large = true,
        Extension = "highschoolhacking",
        Back = "/",
        Slogans = new string[]
        {
            "Like Java, but way better. Way, way, way better.",
        },
        Paragraphs = new string[]
        {
            "C# is an extremely powerul language made by Microsoft that runs on the .NET platform, which is an open source software framework also by Microsoft.",
            "C# isn't a scripting language like the other languages on this site (its code is compiled <em>then</em> run like C++ or Java, rather than evaluated line-by-line like in Python or JavaScript). It has a ton of amazing language features that make it a superb programming language."
        }
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.CSharp.Color, 1),
        Title = "Learn the Language",
        Paragraphs = new string[]
        {
            "These lessons will familiarize you with basic CSharp syntax and the basics of coding. If you've never coded before, this is where the magic happens - you'll learn how to do use all the basic tools to make programs. If you have coded in other languages, skim through these lessons to get used to CSharp."
        },
        Goto = "/CSharp/Learn"
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.CSharp.Color, 2),
        Title = "Delegates and Lambdas",
        Paragraphs = new string[]
        {
            "Delegates are C#'s way of letting you pass Functions around as variables. They combine (some of) the flexibility of JavaScript's first-class Functions with C++'s type safe Function pointers.",
            "Lambdas are small anonymous Functions that you can pass around for ease of use, instead of writing them out."
        },
        Goto = "/CSharp/DelegatesLambdas"
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.CSharp.Color, 3),
        Title = "LINQ",
        Paragraphs = new string[]
        {
            "LINQ stands for <u>L</u>anguage <u>In</u>tegrated <u>Q</u>uery. It's a part of .NET that adds a ton of data manipulation capabilities to the various containers, such as Arrays and Dictionaries. It allows you to manipulate large amounts of data much easier, with significantly less code."
        },
        Goto = "/CSharp/LINQ"
    });
    
    <!--
    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.CSharp.Color, 4),
        Title = "Async / Await",
        Paragraphs = new string[]
        {
            "."
        },
        Goto = "/CSharp/Async"
    });
    -->
}