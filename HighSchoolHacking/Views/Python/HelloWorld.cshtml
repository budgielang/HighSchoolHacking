@using HighSchoolHacking.Models
@using HighSchoolHacking.Models.Pages

@{
    Html.RenderPartial(Section.GetSharedView("HelloWorld"), new HelloWorld
    {
        Color = "purple",
        Language = "Python",
        LanguageDescription = "a bit different (and a lot simpler) than Java, so you want to get used to the syntax",
        PrintFunction = "print"
    });
}