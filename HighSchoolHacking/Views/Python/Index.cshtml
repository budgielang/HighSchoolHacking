@using HighSchoolHacking.Models

@{
    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Languages.Python.Color,
        Title = "Python",
        Extension = "highschoolhacking",
        Large = true,
        Back = "/",
        Paragraphs = new string[]
        {
            "Python is one of the best languages to start coding with, if not the best. It's simple, easy to explain, and less nitpicky than almost every other language.",
            "Start with Learn to get familiar with how to write simple Python code. Once you've completed all the lessons there, go through the activities in order."
        }
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.Python.Color, 1),
        Title = "Learn the Language",
        Paragraphs = new string[] 
        {
            "These lessons will familiarize you with basic Python syntax and the basics of coding, then guide you through a few hacking-style puzzles to find secret codes, show you how to make a basic AI chat bot, and help you generate your own Mad Libs.",
        },
        Goto = "/Python/Learn"
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.Python.Color, 2),
        Title = "Wing IDE",
        Paragraphs = new string[] 
        {
            "Wing IDE is a great Python IDE that lets you run Python code using files on your computer, and without the internet. You should get the free version to do the rest of the activities.",
        },
        Goto = "/Python/WingIDE"
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.Python.Color, 3),
        Title = "File I/O",
        Paragraphs = new string[] 
        {
            "Now that you're developing in WingIDE, you can start having your code read from and write to files. File I/O is how a lot of programs store persistent data on your computer, and a lot of the lessons here will use it.",
        },
        Goto = "/Python/FileIO"
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.Python.Color, 4),
        Title = "Simplebot",
        Paragraphs = new string[] 
        {
            "One of the biggest new technologies right now is natural language, or making software understand and speak like a real person. We're going to make a program that pretends to hold a conversation with you and seems intelligent, but in reality is just parroting back preset phrases."
        },
        Goto = "/Python/Simplebot"
    });

    Html.RenderPartial(Section.ViewFile, new Section
    {
        Color = Section.GetNextColor(Languages.Python.Color, 5),
        Title = "Word Spam",
        Paragraphs = new string[] 
        {
            "Security is a big part of coding. Hacking and hackers are everywhere on the internet, so it's very important people and organizations keep their data secure.",
            "This exercise teaches you how to make a random number guessing game, then through the process of encoding a small message inside a very large text file, and reading similar text files to find encoded messages."
        },
        Goto = "/Python/WordSpam"
    });
}