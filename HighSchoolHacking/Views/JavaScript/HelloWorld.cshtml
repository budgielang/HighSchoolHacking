@using HighSchoolHacking.Models
@using HighSchoolHacking.Models.Pages

@{
    Html.RenderPartial(Section.GetSharedView("HelloWorld"), new HelloWorld
    {
        Color = "blue",
        Language = "JavaScript",
        LanguageDescription = "a bit simpler than Java or C++ but a little trickier than Python, so you want to get used to the syntax",
        PrintFunction = "console.log"
    });
}